<form method="post" action="{{ path('app_gandaal_administration_securite_control_acces_delete', {'id': control_acce.id, etablissement : etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ control_acce.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
