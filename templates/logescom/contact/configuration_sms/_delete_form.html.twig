<form method="post" action="{{ path('app_logescom_contact_configuration_sms_delete', {'id': configuration_sm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ configuration_sm.id) }}">
    <button class="btn">Delete</button>
</form>
