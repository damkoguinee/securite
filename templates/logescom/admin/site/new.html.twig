{% extends 'base_admin.html.twig' %}

{% block title %}Ajouter une nouvelle site{% endblock %}

{% block presentation %}
    <div class="row m-0 p-0">
        {% include "logescom/admin/_nav_admin.html.twig" %}
        
        <div class="col-sm-12 col-md-10 m-0 p-0">
            <div class="container-fluid p-1">
                <h1 class="text-center mb-2 fs-4 text-white bg-dark">Ajouter une nouvelle site</h1>

                <div class="card shadow-sm bg-light">
                    <div class="card-body">
                        {{ include('logescom/admin/site/_form.html.twig') }}
                        
                        <div class="d-flex justify-content-between my-4">
                            <a class="btn btn-light" href="{{ path('app_logescom_admin_site_index') }}">
                                <i class="fa fa-arrow-left"></i> Retour à la liste
                            </a>
                        </div>
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
