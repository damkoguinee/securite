{% extends 'base_admin.html.twig' %}

{% block title %}Types de Pénalité{% endblock %}

{% block presentation %}
<div class="row m-0 p-0">
    {% include "logescom/admin/_nav_admin.html.twig" %}

    <div class="col-sm-12 col-md-10 m-0 p-0">
        {% include "_common/_messages_flash.html.twig" %}

        <div class="card border-0 shadow-lg mt-3 mx-2 rounded-3">
            <div class="card-header bg-gradient bg-dark text-white d-flex justify-content-between align-items-center">
                <h5 class="m-0">
                    <i class="fa fa-shield-alt me-2"></i> Types de Pénalité
                </h5>
                <a href="{{ path('app_logescom_admin_config_penalite_type_new') }}" class="btn btn-outline-light btn-sm">
                    <i class="fa fa-plus-circle me-1"></i> Nouveau type
                </a>
            </div>

            <div class="card-body bg-light">
                {% if config_penalite_types is empty %}
                    <div class="alert alert-warning text-center mb-0">
                        <i class="fa fa-info-circle"></i> Aucun type de pénalité n’a encore été configuré.
                    </div>
                {% else %}
                    <div class="table-responsive">
                        <table class="table table-hover table-bordered align-middle shadow-sm">
                            <thead class="table-dark text-center align-middle">
                                <tr>
                                    <th style="width: 5%">#</th>
                                    <th style="width: 20%">Nom</th>
                                    <th style="width: 35%">Description</th>
                                    <th style="width: 15%">Montant</th>
                                    <th style="width: 10%">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for config_penalite_type in config_penalite_types %}
                                    <tr>
                                        <td class="text-center fw-bold">{{ loop.index }}</td>
                                        <td class="fw-semibold text-dark">{{ config_penalite_type.nom|capitalize }}</td>
                                        <td>{{ config_penalite_type.description ?: '<em class="text-muted">Aucune description</em>'|raw }}</td>
                                        <td class="text-end fw-semibold text-success">
                                            {% if config_penalite_type.montantDefaut %}
                                                {{ config_penalite_type.montantDefaut|number_format(0, ',', ' ') }} 
                                            {% else %}
                                                <span class="text-muted">-</span>
                                            {% endif %}
                                        </td>
                                        
                                        <td class="text-center">
                                            <div class="btn-group btn-group-sm">
                                                <a class="btn btn-outline-success mx-1" 
                                                   href="{{ path('app_logescom_admin_config_penalite_type_show', {'id': config_penalite_type.id}) }}"
                                                   title="Voir les détails">
                                                    <i class="fa fa-eye"></i>
                                                </a>
                                                <a class="btn btn-outline-warning" 
                                                   href="{{ path('app_logescom_admin_config_penalite_type_edit', {'id': config_penalite_type.id}) }}"
                                                   title="Modifier">
                                                    <i class="fa fa-edit"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                {% endif %}
            </div>

            <div class="card-footer bg-white text-center">
                <a href="{{ path('app_logescom_admin_config_penalite_type_new') }}" class="btn btn-primary">
                    <i class="fa fa-plus-circle me-1"></i> Ajouter un type de pénalité
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
