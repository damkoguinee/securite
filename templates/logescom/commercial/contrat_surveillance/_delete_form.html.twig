<form method="post" action="{{ path('app_logescom_commercial_contrat_surveillance_delete', {'id': contrat_surveillance.id, 'site': site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat_surveillance.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
