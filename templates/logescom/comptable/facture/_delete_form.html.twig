<form method="post" action="{{ path('app_logescom_comptable_facture_confirm_delete', {'id': facture.id, site : site.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ facture.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
